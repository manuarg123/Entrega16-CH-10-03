<h2>Registro</h2>
<span class="small text-secondary">Ingresa tus datos para registrarte</span>
<div class="row g-3">

  <form action="/api/usuario/register" method="POST">
    <div class="form-group">
      <label for="nombres">Nombre</label>
      <input
        type="test"
        class="form-control"
        id="nombres"
        name="nombre"
        placeholder="Ingrese un nombre"
      />
    </div>
    <div class="form-group">
      <label for="emails">Email</label>
      <input
        type="test"
        class="form-control"
        id="emails"
        name="email"
        placeholder="Ingrese un email"
      />
    </div>
    <div class="form-group">
      <label for="pass"> Ingrese una Contraseña</label>
      <input
        type="password"
        id="pass"
        name="password"
        required="true"
        class="form-control"
        placeholder="Password"
        aria-label="Password"
        aria-describedby="basic-addon1"
      />
    </div>
    <div class="form-group">
      <label for="roles"> Ingrese el rol</label>
      <select id="roles" name="rol" required="true" class="form-control">
        <option value="USER">USER</option>
      </select>
    </div>
    <button type="submit" class="btn btn-primary">Registrar</button>
  </form>
</div>